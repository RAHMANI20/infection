# Faical Sid ahmed Rahmani(22010400)
# Brice Le Meur(21810250)

** marche à suivre pour executer**:

1) il faut d'abord se placer dans le repertoire 'src'

2) ouvrir un terminal dans src

3) pour compiler :

- javac -d "../build" infection/*.java

4) pour executer :

- joueur aleatoire :  java -cp "../build" infection.Main

- joueur IA :  java -cp "../build" infection.Main <deepBlue> <deepRed> <pruning?>